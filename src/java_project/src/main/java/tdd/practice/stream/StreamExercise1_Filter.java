package tdd.practice.stream;

import java.util.List;

/**
 * Exercise 1: Filter — Find passing students (score >= 60)
 *
 * Traditional loop version (for reference — DO NOT copy):
 * <pre>
 *   List<Student> passing = new ArrayList<>();
 *   for (Student s : students) {
 *       if (s.score() >= 60) passing.add(s);
 *   }
 * </pre>
 */
public class StreamExercise1_Filter {

  public static List<Student> passingStudents(List<Student> students) {
    // TODO: Replace with a stream
    return List.of();
  }
}
