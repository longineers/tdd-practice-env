package tdd.practice.stream;

import java.util.List;

/**
 * Exercise 2: Map — Extract all student names as uppercase strings
 *
 * Traditional loop version (for reference):
 * <pre>
 *   List<String> names = new ArrayList<>();
 *   for (Student s : students) {
 *       names.add(s.name().toUpperCase());
 *   }
 * </pre>
 */
public class StreamExercise2_Map {

  public static List<String> upperCaseNames(List<Student> students) {
    // TODO: Replace with a stream
    return List.of();
  }
}
